[pytest]
testpaths = tests
python_files = test_*.py
markers =
    unit: Unit tests (fast)
    integration: Integration tests with HTTP mocking
    smoke: Visualization smoke tests
addopts = -v --strict-markers --tb=short
